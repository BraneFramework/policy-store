[package]
name = "sqlite-database"
version = "0.1.0"
edition = "2021"
authors = ["Bas Kloosterman", "Tim Müller"]
repository.workspace = true
license.workspace = true
description = "Implements the `DatabaseConnector` for an SQLite backend."


[dependencies]
chrono = "0.4.38"
diesel = { version = "2.2.4", features = ["chrono", "r2d2", "sqlite"] }
serde = "1.0.204"
serde_json = "1.0.120"
thiserror = "1.0.61"
tokio = { version = "1.41.0", default-features = false, features = ["rt", "rt-multi-thread"] }
tracing = "0.1.40"

specifications = { path = "../../spec" }


[build-dependencies]
diesel = { version = "2.2.4", default-features = false, features = ["sqlite"] }
diesel_migrations = "2.2.0"


[features]
default = []
